import 'package:flutter/material.dart';

class ParticleSpec {
  const ParticleSpec({
    required this.angle,
    required this.distance,
    required this.radius,
    required this.color,
  });

  final double angle;
  final double distance;
  final double radius;
  final Color color;
}
